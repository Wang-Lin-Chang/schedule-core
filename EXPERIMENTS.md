# schedule-core 实验记录

> 原则：没实测不声称。每个能力声明带实验编号与对照组；本记录按原始数据核对。

## EXP-1 冷启动 overdue 扫描策略（本包判决）

装置：10 万条记录 × 三种负载（全过期/半过期/罕见过期），固定时钟锚，SQLite 实表。

| 候选 | 全过期 | 半过期 | 罕见过期 | 判决 |
|---|---|---|---|---|
| A 单条 UPDATE 批量翻转 | 779ms | 220ms | 13.6ms | 成本 = 写到期行数的物理下限（WAL） |
| B 惰性虚拟 overdue | 13.5ms | 9.7ms | 7.4ms | 最便宜但状态语义变隐式（invariant/list 需投影） |
| E 分批翻转（1000/批）| 1677ms | 778ms | 15.3ms | 累计更慢——只是把 A 的延迟切碎 |

**判决**：保留 A 的批量翻转语义（显式 scheduled→overdue 转移，invariant 依赖），但把翻转从 `recover()`（同步构造路径）移到统一 sweep 路径——recover 只做租约释放（快路径）。10 万全过期冷启动从"构造器阻塞 779ms"变为"启动即返回，首 sweep 同刻完成翻转"。诚实标注：这是延迟重排（总写成本不变），不是成本消灭。

## EXP-2 派发驱动（三候选对照）

| 候选 | 派发延迟 | 内存 | 时钟回拨 |
|---|---|---|---|
| A 逐记录 timer（官方式）| 0ms | 10 万 timer | 无兜底 |
| B 固定间隔轮询 | p50 0.5~30s | O(1) | 自然免疫 |
| **C 混合（当选）** | **0ms** | **O(1)（单 timer）** | **60s 兜底轮询** |

判决：C 混合——最近到期单 timer + 60s 兜底轮询。10 万条记录 1 个 timer vs 官方式 10 万 timer，派发延迟与官方式相同。

## EXP-3 时钟乱序 fuzz

纯模型 + 假时钟，200 种子 / 7770 次随机操作（创建/前进/回拨/认领派发/崩溃/取消），不变量 I1~I5（状态转移合法/派发不早于锚点/at-least-once/dispatch 单调/租约不双持）：**200 通过 / 0 违反**。首轮曾抓出两个字段生命周期 bug（dispatch 不清租约 = I4 违反；every 回环不清 overdueAt = I2 误判）——实装必须遵守（见源码 dispatch 注释）。

## EXP-4 实装×模型差分

同一随机操作序列同时打到内存模型与 SQLite 实装，操作返回值 + 逐步状态比对：100 种子 / **644 断言 0 失败**。曾抓出 fuzz 漏掉的真语义缺陷（create 的 after 参数把时刻当延迟的双重时钟、every 间隔与锚点混参）。

## EXP-5 时钟工具：IANA 时区 + DST 回转换检测

- Intl.DateTimeFormat 反推偏移 + 回转换还原验证；DST 缺口/重叠歧义 → 拒绝。
- **本包测试覆盖正常转换路径时抓到真 bug**：回转换检测曾与中间量（假锚的墙钟表示）比较——任何非零偏移的正常时刻都被误判为 DST 缺口。修正为与入参 local 本身比较。已回灌真机（dsh-jobs-persisted schedule-tools）。

## EXP-6 解耦行为等价

从框架耦合实现提取为零依赖核心（执行副作用宿主注入），行为等价证据：单元 37 断言 + EXP-3 fuzz 200/0 + EXP-4 差分 644/0 全绿（与原实现同套测试迁移，断言未改）。
